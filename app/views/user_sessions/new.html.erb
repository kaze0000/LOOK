<%= render 'shared/nav' %>
<div class="box-wrapper">
  <div class="box">
    <div class="box-inner">
      <p>ログインすると、MyClosetに自分の服が登録できます。</p>
      <%= form_with url: login_path, local: true do |f| %>
        <div class="form-group">
          <%= f.text_field :email,class: 'input-form', placeholder: 'メールアドレスを入力してください'%>
        </div>
        <div class="form-group">
          <%= f.text_field :password,class: 'input-form', placeholder: 'パスワードを入力してください'%>
        </div>
        <%= f.submit 'ログインする',class: 'button' %>
      <% end %>
      <%# <%= link_to '新規登録はこちら', new_user_path,data: {"turbolinks" => false} %>
      <p>初めてのご利用の方はこちら（新規会員登録）</p>
    </div>
  </div>
</div>
