<%= form_with model: brand_name , url: mycloset_path(clothe), local: false, method: :patch, id:'update-form' do |f| %>
  <div class="form-group">
    <%= f.label :name, 'ブランド名を入力'%>
    <%= f.text_field :name,class: 'input-form'%>
  </div>
  <%= f.fields_for :clothe do |i| %>
    <div class="form-group" id="update-category-<%= clothe.id %>">
      <%= i.label :category, 'カテゴリーを選択'%>
      <%= i.select :category, options_for_select(Clothe.categories.keys, selected: clothe.category), {include_blank: ''}, { class: "select-form" } %>
    </div>
    <div class="form-group">
      <%= i.label :gender, 'ジェンダーを選択'%>
      <%= i.select :gender, options_for_select(Clothe.genders.keys, selected: clothe.gender), {include_blank: ''}, { class: "select-form" } %>
    </div>
  <% end %>
  <%= f.submit '更新する',class: 'button' %>
  <a class="js-edit-modal-close-<%=clothe.id%> mt5" href="">閉じる</a>
<% end %>
